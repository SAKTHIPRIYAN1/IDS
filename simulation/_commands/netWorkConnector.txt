On my terminal
    sudo ip link add veth-host type veth peer name veth-so
    sudo ip link set veth-host up
    sudo ip addr add 172.17.250.1/24 dev veth-host



On my terminal
    ps -au | grep "mininet"

On my terminal
    sudo ip link set veth-so netns 40432 (PID)

On mini -> SO (xterm)
    ip link set veth-so up
    ip addr add 172.17.250.2/24 dev veth-so
    ip route add default via 172.17.250.1





